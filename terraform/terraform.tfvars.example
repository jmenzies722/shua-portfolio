# Copy this file to terraform.tfvars and fill in your values
# DO NOT commit terraform.tfvars to git (it's in .gitignore)

aws_region   = "us-east-1"
bucket_name  = "your-unique-bucket-name-shua-portfolio"
domain_name  = "" # Optional: e.g., "portfolio.yourdomain.com"
acm_certificate_arn = "" # Optional: ACM certificate ARN if using custom domain


